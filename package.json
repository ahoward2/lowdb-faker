{
  "name": "lowdb-example",
  "version": "0.0.1",
  "description": "An example utility for generating data and storing it in lowdb",
  "main": "index.js",
  "author": "Austin Howard <ahoward2@unca.edu>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf build",
    "compile": "yarn clean && tsc -p ."
  },
  "devDependencies": {
    "@faker-js/faker": "6.0.0-alpha.7",
    "@types/faker": "^6.6.9",
    "@types/node": "^17.0.21",
    "lowdb": "^3.0.0",
    "typescript": "^4.6.2"
  }
}
